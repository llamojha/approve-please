# Implementation Plans

Generated by the improve skill on 2026-07-04, from an audit at commit `93d5370`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo verification suite (after plan 001 lands): `npm ci && npm run typecheck && npm run lint && npm test && npm run build`.
Note: `node_modules` may be absent in a fresh checkout — every plan starts with `npm ci`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Test baseline (Vitest) + PR CI | P1 | M | — | DONE (2026-07-09; 63 tests; step 5 completed by orchestrator after pre-existing lint error fixed in c66e265) |
| 002 | Settle deployment story, remove broken release workflow | P1 | S | — (001 recommended first) | DONE (2026-07-09; note: releases v1.0.0/v1.1.0 zips had 7+8 downloads — maintainer signed off on server-only anyway) |
| 003 | Leaderboard through API route + input bounds + RLS runbook | P1 | M | 001, 002 | DONE (2026-07-09; 74 tests; maintainer must run docs/leaderboard-security.md RLS steps in Supabase) |
| 004 | Deterministic initial state + honest error boundary | P1 | S | 001 | DONE (2026-07-09; 68 tests; in-browser /game hydration check pending) |
| 005 | Finish false-positive tracking feature | P2 | M | 001 (before 006) | DONE (2026-07-10; amended: dead claimedKind removed; note: StatsPanel doesn't show falsePositives — follow-up decision) |
| 006 | Dedupe screens + leaderboard client code | P2 | M | 003, 005 | TODO |
| 007 | Lazy-load per-language template packs | P3 | L | 001 | DONE (2026-07-09; /game first load −404.5KB (−39%); day-1 pacing playtest pending) |
| 008 | Compress oversized social-card assets | P3 | S | — | DONE (2026-07-09; PNG→JPEG 2.76MB→289KB + 1.87MB→264KB; browser/OG-unfurl check pending) |
| 009 | Dependency advisories + tsconfig target | P3 | S | 001 | DONE (2026-07-09; 0 high/critical; residual postcss moderate documented — next 16.2.10 still pins postcss 8.4.31) |
| 010 | Learning-mode curriculum design spike | P3 | M | 001 (007 first if both run) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always**: it creates `npm test`, which every later plan uses as a verification gate, and CI to catch executor mistakes.
- **002 before 003**: 003 routes all leaderboard traffic through the server API route; that only makes sense once 002's "server deployment is canonical" decision stands. If 002 is rejected (static export wanted), 003 must be re-planned around anon-key writes + Supabase-side constraints instead.
- **005 before 006**: 006 relocates `formatFalsePositiveReason`, which 005 gives its first real caller; running them in reverse creates churn.
- **003 before 006**: 006 dedups the two leaderboard read call sites after 003 has pointed them at the API route.
- **007 and 010 both touch `hooks/usePRSpawner.ts`**: run 007 first; 010 has a STOP condition covering the interaction.

## Findings considered and rejected

- **"Missing tutorial slide-6 image" (plan 008, withdrawn 2026-07-09)**: the original audit claimed `public/tutorial-slide-6-placeholder.png` didn't exist and `pages/index.tsx` referenced a 404. Wrong — the file exists (added in `18f2cb2`, present at the audit commit) and slide 6 renders. Plan 008 was amended to compression-only.
- **`uniqueId` collision risk** (`utils/helpers.ts:19` — 4 base36 chars for PR ids): collision odds over a full run are negligible (~50 draws vs 1.7M space) and ids are also prefixed with templateId+day+index. Not worth doing.
- **`advanceToNextDay` missing the velocity game-over check** (`context/GameContext.tsx:436`): unreachable in practice — every meter mutation already passes through `maybeGameOver` during WORK. Documented via a characterization test in plan 001 instead of a code change.
- **tsconfig `target: es5` as a standalone finding**: folded into plan 009.
- **Landing page `<style jsx>` block (`pages/index.tsx:299`)**: already a documented in-progress migration in `.kiro/steering/tech.md` ("CSS Migration (In Progress)") — tracked by the maintainer; re-planning it here would duplicate stated intent.
- **Clock throttling when the tab is backgrounded** (`hooks/useGameClock.ts` setInterval): arguably a feature (game slows when not watching); no player-facing complaint evidence. Revisit only if pacing bugs get reported.
- **Wave-spawn design, dev-only spawner logging, manifest system**: intentional per `agent.md` non-negotiables; not findings.

## Direction options not planned (for the maintainer to weigh)

- **Run persistence / "continue"**: nothing about a run survives a reload (only locale + theme are stored). A day-boundary save of `GameState` is cheap since state is one serializable object. Natural follow-up to plan 004.
- **Day-rule progression**: README design pillar #2 ("increasing rule complexity per day") and the existing `RulebookPanel` are still cosmetic — `getDayMantra()` isn't even day-indexed. Connecting daily mantras to real scoring modifiers is the core Papers-Please mechanic still missing. Plan 010's doc is the right place to note interactions if pursued.
